那我现在开始报告第20组的这次作业

Department.xsd：（Department Schema）
-- code line 11번부터 보여주기 ： 包含了4个元素信息（部门编号，部门名称，部门类型，部门电话）
-- code line 32번부터 보여주기 ： 그리고 部门类型은 院,系,直属部门,行政部门중 하나를 선택할 수 있다

PersonInfo.xsd：（PersonInfo Schema）
-- code line 11번부터 보여주기 ：包含了5个元素信息（姓名，性别，出生日期，身份证号，部门）
-- code line 6번, 17번 ：而且使用了Department设计来描述‘部门’元素的信息

Course.xsd：（Course Schema） << 원래코드에 변동'있음' >>
-- code line 7번부터 보여주기 ： 包含了3个元素信息（课程编号，课程类型，课程名称）
-- code line 23번부터 보여주기 ： 그리고 课程类型은 选修课，必修课중 하나를 선택할 수 있다

ScoreList.xsd：（ScoreList Schema） << 원래 코드에서 변동'없음' >>
这是课程成绩列表
包含了4个信息（学号，得分，课程编号，成绩性质）그 중에
-- code line 44，45번 ：学号，得分是元素
-- code line 38，39번 ： 课程编号，成绩性质是属性

----------------------------------------------------------------
<순서대로 깊이있게 설명하자면>
29번에 : 课程成绩列表 원소가 있고 이 类型은 23번 줄에
23번에 : 이 课程成绩列表类型에 속한 원소 课程成绩는 课程成绩类型이며 이 类型은 32번 줄에
32번에 : 이 课程成绩类型에 원소 成绩와 두 개의 속성 课程编号, 成绩性质이 있다
  각 속성에 대한 형식은 课程编号 -> Course.xsd , 成绩性质 -> 13번
  그리고 원소 成绩는 成绩类型이며 이 类型은 42번

42번에 : 이 成绩类型에 원소 学号, 得分가 있다
  각 원소에 대한 类型은 学号 -> Studendt.xsd , 得分 -> 49번
-----------------------------------------------------------------

Student.xsd：（Student Schema） << 원래코드에 변동'있음' >>

-- 12번에서 14번으로 ：主要分两个信息（学生个人信息， 学生分数）
-- 21번에서 28번으로 ： 首先‘学生个人信息’元素里的子信息有 4个(个人信息，学号，入学年级，学籍状态）
-- 8번 ：其中’个人信息‘元素的类型是从PersonInfo.xsd的
-- 30번에서 47번으로 ： 别的元素类型是都在这儿

-- 50번에서 54번으로：还有一个主要元素‘学生分数’是，类型是从ScoreList.xsd来的，
这元素里的信息就是课程成绩列表

根据Student Schema来表示xml形式的话，就是这样的。

StudentList.xsd：
通过引用Student.xsd 包含了1个信息（Student）


这是我们组完成的这次设计作业。谢谢
