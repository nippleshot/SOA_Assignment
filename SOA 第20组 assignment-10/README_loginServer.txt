loginServer是使用SFJ(Start from Java)方式构建的新服务功能
    - src.Server是用来开启服务的
        * 开启服务器后在http://localhost:8088/loginServer/service/LoginInterface?wsdl可以查看wsdl文件 (在Image_loginServer文件里也有截图)
    - src.TestClient用于测试
    - 在Image_loginServer文件可以看src.TestClient的输出结果
